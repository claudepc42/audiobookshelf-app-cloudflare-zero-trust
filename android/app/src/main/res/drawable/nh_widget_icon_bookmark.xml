<!-- Same "bookmark" glyph the in-app mini player uses (material-symbols
     "bookmark", filled) — same shape, opened via a widget tap since RemoteViews
     can't render the actual BookmarksModal directly (see
     NanoHiveMediaPlayerWidget.kt's widgetActionPendingIntent). -->
<vector android:height="24dp" android:tint="#FFFFFF"
    android:viewportHeight="24" android:viewportWidth="24"
    android:width="24dp" xmlns:android="http://schemas.android.com/apk/res/android">
    <path android:fillColor="@android:color/white" android:pathData="M17,3H7C5.9,3 5,3.9 5,5v16l7,-3l7,3V5C19,3.9 18.1,3 17,3z"/>
</vector>
